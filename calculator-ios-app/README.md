### Cordova app for the calulator

To start, install cordova globally

```
npm install -g cordova
```

Run inside this directory:

```
cordova platform add browser ios
cordova build browser
cordova run browser
```
